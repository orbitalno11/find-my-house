<!DOCTYPE html>
<html lang="en">

<head>
    <title>แก้มูลนิธิเพื่อสัตว์</title>
    <% include partials/header %>
</head>

<body>
    <% data %>
    <div class="banner" style="background-image: url(/img/userBanner.jpg);">
        <h1 class="bannerText">ระบบบริหารและจัดการข้อมูล</h1>
        <br>
    </div>

    <% include partials/navbar %>
    <a id="pageActive" class="nav-item active nav-link" href="#">แก้มูลนิธิเพื่อสัตว์</a>
    <% include partials/navbar2 %>

    <% include partials/adminnav %>

    <div class="container" style="margin-top: 1em; margin-bottom: 2em;">
        <form method="POST" action="/foundation/edit/<%= data._id %>" enctype="multipart/form-data" style="background-color: #FCF3CF; padding: 2em; border-radius: 0.5em;">
            <div class="row">
                <div class="col-sm-12 col-md-6 col-lg-6">
                    <label for="name">ชื่อมูลนิธิ</label>
                    <input id="name" name="name" class="form-control" type="text" required value=<%= data.name %>>
                    <label>ข้อมูลมูลนิธิ</label>
                    <textarea class="form-control" id="description" name="description" rows="5" required><%= data.description%></textarea>
                </div>
                <div class="col-sm-12 col-md-6 col-lg-6">
                    <div id="uploadUserPic">
                        <label class="file-upload btn btn-primary">
                            <input type="file" id="picture" name="picture" hidden>
                            เลือกรูปภาพ
                        </label>
                        <!-- <label>รูปภาพควรมีขนาด 640px X 480px</label> -->
                        <br>
                        <div style="width: 100%;height: 180px; text-align: center;">
                            <img id="img-upload" src="/image/foundation/<%= data._id %>" style="width: 240px ;height: 180px;">
                        </div>
                    </div>
                </div>
            </div>

            <div class="row" style="margin-top: 1em;">
                <div class="col-12">
                    <label>ช่องทางติดต่อ</label>
                </div>
            </div>

            <div class="row" style="margin-top: 1em;">
                <div class="col-12 col-lg-auto">
                    <label>เบอร์โทรศัพท์</label>
                </div>
                <div class="col-12 col-lg-auto">
                    <input type="text" class="form-control" id="phone" name="phone" placeholder="098-765-4321" required value=<%= data.contact.phone %>>
                </div>
            </div>

            <div class="row" style="margin-top: 1em;">
                <div class="col-12 col-lg-auto">
                    <label>ที่อยู่</label>
                </div>
                <div class="col-12 col-lg-auto">
                    <input type="text" class="form-control" id="address" name="address" placeholder="098-765-4321" required value=<%= data.contact.address %>>
                </div>
            </div>

            <div class="row" style="margin-top: 1em;">
                <div class="col-12" style="text-align: center">
                    <div class="btn-group" >
                        <button type="submit" class="btn btn-warning">แก้ไข</button>
                        <a href="/foundation/list" class="btn btn-danger">ยกเลิก</a>
                        <a class="btn btn-outline-danger btn-dark" href="/foundation/delete/<%= data._id %>">ลบมูลนิธิ</a>
                    </div>
                </div>
            </div>
        </form>
    </div>
    <%include partials/footer %>
</body>

</html>